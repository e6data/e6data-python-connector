[pytest]
testpaths = test
python_files = test_*.py tests.py tests_grpc.py
addopts = -ra
markers =
    integration: requires explicitly configured real services
    synthetic: existing isolated test doubles, not backend qualification
